{% extends 'base.html.twig' %}

{% block title %}Choix{% endblock %}

{% block body %}
    <h1>Choix</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ choix.id }}</td>
            </tr>
            <tr>
                <th>Correction</th>
                <td>{{ choix.correction }}</td>
            </tr>
            <tr>
                <th>Texte</th>
                <td>{{ choix.texte }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('choix_index') }}">back to list</a>

    <a href="{{ path('choix_edit', {'id': choix.id}) }}">edit</a>

    {{ include('choix/_delete_form.html.twig') }}
{% endblock %}
